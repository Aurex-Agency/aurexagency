

## Update Favicon to Aurex Logo

Copy the Aurex logo (`src/assets/aurex-logo-main.png`) to `public/favicon.png` so the browser tab displays the Aurex Agency logo.

### Steps

1. Copy `src/assets/aurex-logo-main.png` to `public/favicon.png` (overwriting the current favicon)
2. No changes needed to `index.html` since it already references `/favicon.png`

This is a simple file copy -- no code changes required.

